# Dependencias del proyecto trakt-to-letterboxd
# Instalar con: pip install -r requirements.txt

# Peticiones HTTP asíncronas# Para hacer peticiones HTTP a Trakt
httpx==0.27.2

# Automatización del navegador UC Mode para brincarse Cloudflare
seleniumbase==4.35.4

# Para CI/GitHub Actions también se necesita instalar los navegadores:
#   playwright install chromium
#   playwright install-deps chromium   ← en Linux instala librerías del sistema
