# Dependencias del proyecto trakt-to-letterboxd
# Instalar con: pip install -r requirements.txt

# Peticiones HTTP asíncronas a la API de Trakt
httpx==0.27.0

# Automatización del navegador (login + upload en Letterboxd)
playwright==1.44.0

# Utilidades de asyncio (ya viene con Python 3.10+, pero por claridad)
# asyncio es parte de la stdlib

# Para CI/GitHub Actions también se necesita instalar los navegadores:
#   playwright install chromium
#   playwright install-deps chromium   ← en Linux instala librerías del sistema
